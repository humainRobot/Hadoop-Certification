# When running spark-shell after spark download, the system will consider the local file system
# If we want to access the HDFS we have to copy the files in the conf folder under /etc/spark 
# into the directory of the new spark.

#spark-shell

scala> sqlContext.sql(query).collect().foreach(println)
